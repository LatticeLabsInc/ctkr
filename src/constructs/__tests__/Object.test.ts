import { describe, it } from 'vitest';

describe('Object', () => {
  describe('createObject', () => {
    it.todo('creates an object with a unique ID');
    it.todo('associates the object with a category');
    it.todo('accepts optional name and metadata');
  });

  describe('objectsEqual', () => {
    it.todo('returns true for the same object');
    it.todo('returns false for different objects');
  });
});

